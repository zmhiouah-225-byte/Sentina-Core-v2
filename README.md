Si vous n'avez pas de caméra ou de piscine sous la main, vous pouvez tester le code directement sur le fichier vidéo mp4 inclus dans ce dépôt :

1. Ouvrez le fichier `projet.py`.
2. Modifiez la ligne de la source vidéo (cv2.VideoCapture) :
   **Remplacez :** `cap = cv2.VideoCapture(0)`
   **Par :** `cap = cv2.VideoCapture('test.mp4')`(lle code est déja remplacé par cap = cv2.VideoCapture('test.mp4'))
3. ouvrir le terminal est ecrire "python projet.py" pour activé le systéme
4. attender quelque seconde aprés uvrire google edge et taper http://127.0.0.1:5000
5. users_db = {
    "saidia@gmail.com": "123456
   or :
   "prof": "saidia2026
tapper email est le cde dans le site et tester.

